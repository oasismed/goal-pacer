# Ferramentas de desenvolvimento com versão fixa. O app roda só com a biblioteca padrão do Python 3.9+:
# nada daqui entra na instalação. Uso local (uv): uv run --no-project --with-requirements requirements-dev.txt python dev/verificar.py
pytest==9.0.3; python_version >= "3.10"
# o pytest 9 não roda no 3.9: lá fica o 8.3.5, sem a correção do PYSEC-2026-1845 (/tmp/pytest-of-<usuário> previsível;
# risco só em máquina com outros usuários locais). docs/divida-tecnica.md, D-08
pytest==8.3.5; python_version < "3.10"
coverage==7.16.1
ruff==0.16.7
ty==0.0.80
complexipy==8.0.1
vulture==2.16
shellcheck-py==0.11.0.1
pip-audit==2.10.1
